COLLEGE PAGE — TWO SECTION REDESIGNS
"The Placement Gap" + "A Complete Placement Intelligence System"
Goal: cut the text by ~70%, let visuals carry the argument, make it 
feel like a product from a company that ships fast, not one that 
writes a lot.

═══════════════════════════════════════════════
SECTION A — "THE PLACEMENT GAP"
Currently: 3 dense paragraph cards. Redesign as a single visual 
argument with three supporting proof points, not three essays.
═══════════════════════════════════════════════

CONCEPT: Instead of three equal text blocks, build this as ONE large 
visual statement with three small annotated evidence points beneath 
it — like a headline claim followed by footnotes, not three parallel 
arguments competing for attention.

TOP — THE CLAIM (large, does the emotional work alone):
  Eyebrow: "THE PLACEMENT GAP" — small label.
  Headline, two lines, large scale:
    "Your students are capable."
    "Employers just can't see it yet." (second line gets the accent 
    treatment — color/weight shift on "see it yet.")
  ONE line beneath, not a paragraph:
    "Good interviewers get placed. Good performers get missed."
  That's the entire top block. No further explanation paragraph.

VISUAL CENTERPIECE (replaces the need for explanation):
  A simple before/after or split visual directly below the headline — 
  something like two small side-by-side abstract "pools" of dots/
  avatars: LEFT labeled "Seen by employers" (small cluster, tight), 
  RIGHT labeled "Actually capable" (much larger cluster) — the visual 
  gap between cluster sizes IS the argument. No paragraph needed to 
  explain "there's a gap" when the eye can see the gap.
  Style this abstractly (dot clusters, not literal illustrated people) 
  so it reads as data-driven, not cartoonish.

BOTTOM — THREE EVIDENCE CHIPS, not three paragraph cards:
  Redesign each of the 3 current blocks as a compact horizontal chip/
  row, not a tall card:
  
  Each row: small numbered marker + a single bold micro-headline 
  (5-8 words max) + optionally ONE short supporting phrase (under 
  12 words), NOT the current 2-3 sentence paragraph.
  
  01 — "Last year's numbers don't predict this year's batch."
       (drop the CTC/brochure paragraph entirely — the headline 
       alone carries it)
  
  02 — "Recruiters default to names they already know."
       (drop "hiring teams have limited bandwidth..." — implied)
  
  03 — "No shared yardstick across 40,000+ institutions."
       (keep the number — it's the only stat worth keeping — drop 
       the rest)
  
  Layout: three rows stacked tightly OR three columns side by side, 
  each just icon/number + bold line + thin line — should read in 
  under 2 seconds each, total section scan time under 8 seconds.

CLOSING LINE (small, sits alone, replaces the current closing 
sentence — keep this one almost verbatim, it's the best line in the 
section):
  "An objective score proves a college is better than its reputation 
  suggests."
  Style this distinctly — maybe italic, or set apart with more 
  whitespace than anything else in the section — it's the punchline, 
  give it room to land alone rather than trailing directly under the 
  three evidence rows.

REMOVE ENTIRELY: the "01/02/03 · [context label]" small-caps sub-
headers as currently styled (they add visual noise without adding 
meaning) — fold that context into the icon or number treatment 
instead, not as separate repeated label text on every row.

NO CTA in this section — this section's job is to create tension, 
not convert. Let the next section (the feature grid / intelligence 
system) carry the CTA instead. Adding a button here undercuts the 
"problem" framing by rushing to a sale too early.

═══════════════════════════════════════════════
SECTION B — "A COMPLETE PLACEMENT INTELLIGENCE SYSTEM"
Currently: strong hero card + reasonable 6-card grid, but text-heavy 
per card and the grid feels like a spec sheet, not a product tour.
═══════════════════════════════════════════════

KEEP THE STRUCTURE (headline → hero visual card → 6-item grid) — 
it's the right shape. Tighten and elevate each part.

HEADLINE: Keep as-is, it's already tight and good — one line, no 
change needed.

HERO CARD — visual is strong, copy needs trimming:
  Keep the photo + floating rank-card overlay, keep the accent word 
  treatment on "national rank."
  TRIM the body paragraph from 2 sentences to 1 short line:
    Current: "Each student knows where they stand against the 
    country, not just their batch. The college gets a number 
    employers already use to decide who to call." 
    → Cut to ONE line: "Ranked against the country — not just the 
    batch." (the "employers already use this number" claim can move 
    into a small badge/tag on the floating card itself instead of 
    living in a sentence — e.g. a tiny label near the rank card: 
    "Used by employers" — visual proof instead of stated claim).
  Keep the percentile bar at the bottom — that's a nice touch, no 
  change needed there.

6-CARD GRID — this is the section that most needs redesign:

  CURRENT PROBLEM: every card has the same shape — icon, bold title, 
  2-line description — it reads as a spec list, six identical rows 
  of text with no visual hierarchy or rhythm.

  REDESIGN APPROACH: break the uniformity. Not all 6 features are 
  equally important — treat this as a bento layout instead of a 
  uniform grid, and cut every description to a single short phrase 
  (under 10 words), never a full sentence.

  BENTO LAYOUT (6 items, uneven sizing):
  
    Make "Batch analytics dashboard" and "Gap map" the two largest 
    tiles (these are the core product, give them 2x visual weight) 
    — each could include a tiny inline visual snippet instead of 
    just an icon: e.g. the dashboard tile shows a miniature bar-chart 
    shape in the corner, the gap map tile shows a tiny layered-strata 
    icon or mini heat-strip — a glance-able visual mnemonic, not just 
    a generic icon.
    
    The remaining 4 (Employer-pool visibility, AI batch insight, 
    Year-over-year tracking, Exam-window control) sit as smaller, 
    equal-weight tiles — icon + short title + one short phrase, no 
    more.

  COPY TRIM per card — cut every 2-line description down to a single 
  short phrase:
    "Batch analytics dashboard" → "Live cohort view, drill to any 
    student."
    "Employer-pool visibility" → "Your Band A/B students, visible 
    to 50+ employers."
    "AI batch insight" → "Plain-English summary of where your batch 
    stands."
    "Gap map" → "Layer-by-layer weak points, batch-wide."
    "Year-over-year tracking" → "This year's batch vs. last year's — 
    same rubric."
    "Exam-window control" → "You set the dates. We run the rest."

  Hover state on each tile: subtle lift + the icon or mini-visual 
  gets a small motion cue (icon nudges, mini-chart bars animate 
  slightly) — small enough to feel premium, not distracting.

CTA — add ONE here, since this section (not the gap section) is 
where the reader is ready to act:
  A single clear CTA beneath the grid: something like "See a sample 
  batch report →" or "Request a demo cohort →" — ghost or outline 
  style, understated, not a loud filled button — this section has 
  already done its convincing through the visuals, the CTA should 
  feel like a quiet next step, not a hard sell.

═══════════════════════════════════════════════
OVERALL PRINCIPLE ACROSS BOTH SECTIONS
═══════════════════════════════════════════════

Every sentence that currently explains what a visual could show 
instead gets cut. Test: if a claim can be shown as a number, a 
comparison, a mini-chart, or a badge instead of a written sentence, 
it must be. Text remaining on the page should function as labels 
and headlines only — paragraphs are the failure mode being corrected 
here, not a formatting detail.